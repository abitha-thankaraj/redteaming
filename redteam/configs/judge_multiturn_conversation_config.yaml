seed: 69

# Dirs
repo_dir: ???
data_dir: /data/group_data/rl/datasets/redteaming

chat_completion:
  url: None
  model: gpt-4
  temperature: 1.

multiturn_conversations_fname: ${data_dir}/gen_eval_multiturn_attacks/Mistral-7B-Instruct-v0.1_test_evaluated_multiturn_responses_16-55-1722459320.json

# directory to save the generated multiturn prompts
# For now we try only harmbench
out_dir: ${data_dir}/gen_judge_multiturn_conversation
save_file : ${out_dir}/${chat_completion.model}_judge_generated_multiturn_conversations_${now:%H-%M-%s}.json 
